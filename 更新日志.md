## 1.7.2

### 🚀 新功能

#### FreeMake 自定义布局编辑器
- **按键框圆角 + 节点级边框厚度**：节点的「圆角半径」大于 0 时，改由程序化生成的圆角矩形绘制，而不再使用直角的九宫格贴图（九宫格无法把角磨圆），并可选独立的描边宽度。半径上限为短边的一半；边框厚度为 0 时仍使用贴图自带的九宫格边框。编辑器画布同步复刻该形状。
- **文字描边与阴影**：TMP 的 SDF 描边与 underlay 阴影现在可配置，且按键标签与按压计数分别独立（大号标签与小号计数很少需要同样的处理）；节点级可用「自定义文字样式」覆盖，开启时会先用当前全局值填充，因此开启动作本身绝不改变已渲染的文字。样式按解析结果缓存，且绝不改写字体资源自带的材质（改写会让样式泄漏到游戏内所有 TMP 文本）。现有配置观感不变：阴影默认值精确复刻此前写死的黑色 0.5、偏移 1,-1 的 underlay。
- **视频按键节点**：给图片节点填上视频路径即播放视频（mp4/mov/webm/avi/wmv/m4v）——通过 VideoPlayer 渲染到 RenderTexture，播放期间取代静态图片，同时保留绑定、计数、按压图片、不透明度、雨滴与层级。播放器跨布局重建存活（每次微调设置都会重建，否则解码器会重启并出现可见黑闪），仅当节点消失或 Mod 拆解时释放。相对路径在 `CustomImages\\` 下解析，文件缺失或格式不支持时退化为静态图片，而非变成看不见的节点。
- **分享配置包（.jkv）**：把当前配置导出为 ZIP，内含配置 JSON、包信息以及全部引用到的图片/视频，并把节点路径改写为纯文件名。导入时把资源解压进 `CustomImages\\`（绝不覆盖已存在文件，并带目录穿越防护）且**永远新建一套配置**——同一个包导入两次会得到两套配置，而不是静默替换。包内记录导出时的画布宽度，导入时按本机宽高比缩放节点 X / 宽度 / 雨滴偏移 X；由于 1080 参考高度固定，Y 与高度绝不改动。
- **命名按压缓动**：按压动画新增 27 项缓动选择器（linear、smoothstep/smootherstep、Penner 的 sine/quad/cubic/quart/quint/expo/circ/back 系列），全局显示页控件与节点级覆盖旁都带曲线预览，并新增可调时长。默认值即 `linear` + 80ms——与此前写死的行为完全一致，现有配置观感不变。缓动作用于归一化进度之后再插值，故 ease-out 曲线仍会精确落在目标缩放值上。

### 🐞 问题修复
- **修改某个组的计数不再覆盖其它所有组的总数**：强制刷新路径（`RefreshAllCountDisplay`、`RefreshKpsTotalLabels`）会把**全局** `TotalCount` 写进每一个 Total 面板。而逐帧刷新逻辑一旦认为某组的值「已显示」就会跳过该组，于是错误的数字会永久留存，而不是下一帧自动纠正。两条路径现在都改为按面板自己所属的组刷新——逐帧路径本就是按组取值的，故分组面板只会被这两处强制刷新污染。

## 1.7.1


### 🚀 新功能

#### FreeMake 自定义布局编辑器
- **节点式自定义布局**：新增「自定义」布局样式与独立游戏内编辑器弹窗（布局页进入）。按键、若干 KPS/Total 面板、每组最多 8 张图片在画布上自由摆放，画布绘制真实屏幕范围作为参照。编辑遵循社区按键显示编辑器流行的 FreeMake 范式：绘制序命中测试、点击拣选优先保持重叠栈中的已选中项、双击在堆叠命中间循环、拖拽叠加对齐吸附（对其它节点与屏幕边/中心；屏幕恒定阈值，Alt 临时关闭，Shift 锁轴）、"实际对齐才显示"的参考线、框选、Shift+点击选中锁定背景图。全部为本仓库原创 Unity IMGUI 实现。方向键微调、Del 删除、Ctrl+C/V 复制粘贴（偏移逐次递增）、Ctrl+Z/Y 撤销重做（快照时间线 + 连发合并窗口）。
- **内置布局预设**：「预设」按钮提供 12K/16K/20K/10K/8K/14K/24K/108K——把固定布局的硬编码排布重建为可编辑节点（含发射线雨滴偏移），并继承当前配置的键位、每键计数与自定义文本。两种去向：落进克隆自当前配置的**新建 Profile**（默认，非破坏），或**追加进当前画布**（自动避让重叠）。每次应用自动创建自己的图层组。12K/20K 的宽窄变体跟随「标准按键宽度」开关。
- **图层组**：命名可见性批次——一个开关整组显隐（隐藏的节点不渲染、不响应、不计数）。组可携带自己的 KPS/Total 面板，多套完整布局共存于一个配置且统计独立。**按组节点预算：每组 112 按键类 + 8 图片**。空组自动清理；组名复用最小空闲编号；图标按钮带悬停提示。
- **节点级覆盖**：显示页与雨线页的每个设置都有节点级逃逸口（关闭 = 跟随全局/排设置）——背景/描边/文本颜色（含按压变体）、自定义与按压文案、字号、隐藏文字/计数、雨滴宽/高/速度、XY 偏移、参数排映射、双色渐变、阴影、描边、顶部渐隐、松开淡出、按压缩放（开关+数值）、计数器弹跳（贝塞尔/幅度/时长）、KPS/Total 文本布局（居中/堆叠/仅数值）、深度、每键 KPS、鬼键捕获。多选编辑显示活动节点的值（画布青色框标注）、改动应用全选、值不一致显示 `—`。
- **多块 KPS/Total 面板**：每个图层组一对（随组显隐）；未分组面板保持全局单对语义。组面板只统计本组按键——按组按压队列算 KPS、按组节点计数求和算 Total。

#### 回放按键同步（TogetherBootstrap 类回放）
- 该类回放（TGT）直接驱动游戏判定，回放按键**不经过任何输入层**（已实测确认：回放期间 OS 键状态表、Unity Input、游戏异步输入掩码与键事件队列全部无动静），任何轮询输入的按键显示器原理上都看不到回放按键。引导器转而在启动时检测名为 "KeyViewer" 的特定按键显示器 mod，对其输入方法打 Harmony 补丁以注入回放录制的真实按键。本 mod 为此内置了兼容垫片：一个极小的内嵌程序集，程序集名、mod 入口与公开输入 API 逐字镜像该 KeyViewer 表面（不含其任何功能）。回放时，回放系统自己的补丁把录制的原始手法经垫片送入，`KeySource` 经反射接入——画面实时还原录制时的手法，计数/KPS/雨滴与物理按压行为完全一致。
- 不产生额外 mod 列表条目：垫片以资源形式内嵌于 JipperKeyViewer.dll、初始化时加载；若已安装独立的 KeyViewer mod 则优先使用它、内嵌垫片不加载。物理按键仍走 Unity Input，行为与 1.7.0 完全一致。

#### 持久化与稳定性
- **配置经 Newtonsoft 持久化**（Fields 契约 + Unity 结构体转换器）：运行时 JsonUtility 会丢弃类数组字段、静默丢失整套布局；旧过渡格式（内嵌 JSON 字符串）自动导入；不可解析文件先备份为 `*.corrupt` 再回退默认，绝不覆盖原件。
- **已知行为**：旧版本读到 Custom 配置会按枚举钳制回落 Key16。

#### 其它
- 「标准按键宽度」开关统一宽窄混排后排为 50px；108 键全键盘支持整块移动滑块与统一配色；扁平框 KPS/Total 文本可居中/堆叠；每键字号；千分位计数格式化；主播模式。

### 🔍 全面代码审计（六路并行审查，30 项修复）
- **陈旧文本缓冲写入错误设置（严重）**：颜色选择器/滑块文本框按位置序号（`cpi_N`/`fsf_N`）命名，缓冲条目在控件消失（折叠/切标签）后存活——下一个继承该序号的字段在首次点击时回显陈旧半输入文本并解析提交到**另一个**设置（如半截 `#ff` 静默改掉 Outline 颜色）。现以逐 pass 已绘制控件集合回收未绘制条目。
- **改键捕获劫持点击/热键**：武装态无取消路径、不滤鼠标、跨标签/关窗存活——点击任意设置控件会把 Mouse0 绑进槽位，ESC 绑入 Escape，按设置热键关窗会把热键自己绑进去。现过滤鼠标、ESC 取消、loader 上报窗口可见性（捕获前查询）、loader 设置热键永不参与捕获（同帧保护——MelonLoader 下完全生效；UMM 自身显隐热键未对 Mod 暴露，该路径留有少量同帧残余）。离开按键页自动解除武装、SetupKey 所有出口复位状态机。
- **损坏 Profile 被默认数据静默覆盖**：`LoadProfileFromMeta` 无视 `LoadProfile` 返回值，可解析但截断的 JSON 留下半默认 Data，下一次高频保存即覆盖用户文件。现加载时校验，不可解析的文件备份为 `*.corrupt`；"短但存在"的 `Count`（旧版本合法的 `Count[36]`）就地重定长度而非拒绝，保住 V3→V4 迁移路径。
- **越界布局枚举锁死界面**：手改 `"KeyViewerStyle": 42` 走到 `GetLayout` 的 throw——覆盖层半构建后逐帧 NRE，设置窗口经 `KpsTotalIsSlim` 一同崩溃，无法从 GUI 自救。现加载时钳制枚举到合法范围；`GetLayout` 回落 Key16 而非 throw。
- **截断绑定数组使按键页每帧崩溃**：`key8`…`GhostKey24` 只有 null 检查无长度校验（与此前为 `Count`/`key108` 修过两轮的坑同类）。`EnsureSettingsArrays` 现按默认重建所有长度不符的 KeyCode/文本数组；绑定行补齐对称边界守卫。
- **场景加载后字体被静默切换**：`OnSceneLoaded` 剔除死字体但不重建 `TryLoadResources` 里一次性构建的名称→索引映射；`RestoreFontOnce` 拿过期索引解析存储的字体名，切到错误字体并持久化。现每次剔除后重建映射。
- **字体资产泄漏**：`fontList.Clear()` 丢弃旧 `TMP_FontAsset`（图集纹理/材质）而不销毁——加载器级开关（UMM 关→开）或 bundle 失败重试每次泄漏一整套。现清理前销毁；`CreateFontAsset` 返回 null 不再入列。
- **FileBased 损坏 PNG 渲染为噪点**：忽略 `ImageConversion.LoadImage` 的 false 返回（2×2 垃圾纹理套 11px 九宫格边框），异常路径每次泄漏一张纹理。现检查返回值、所有失败路径释放纹理、小于边框和的图按损坏拒绝。
- **V3→V4 迁移在 Key24 时跳过其它 Profile**：当前配置为 Key24 的早退整体跳过 `MigrateAllProfileFiles`，而 meta 版本门控永不再补跑——其它 Profile 的脚键计数永久留在旧 20 基线。
- **V3→V4 迁移抹掉部分刚迁完的脚键数据**（既有缺陷）：复制后按**全长度**清除旧脚键区间，而目标区间 [24, 24+fs) 与之重叠——footSize 为 8 时清零 4 个刚迁入的计数/颜色，为 16 时清零 12 个。清除现在只覆盖新旧基线之间的间隙 [20, 24)。在线迁移与批量 Profile 循环两处同修。
- **编辑 KPS 标签时数值卡在 0**：`RefreshKpsTotalLabels` 硬编码 "0" 且未失效 `lastKps` 缓存——匀速按键下数值框无限期显示错误的 0。现写入实时值并强制下帧重写。
- **全键盘 KPS/Total Y 滑块方向相反**：这两个框是全 Mod 唯一"Y=1=顶"的地方；现与主键盘/脚键位置滑块一致（0=顶，1=底）。**v6 设置迁移**对存量值一次性翻转（含批量处理所有 Profile），已有摆放保持屏幕位置不变。
- **计数文本的每键字号从未生效**：`UpdateAllFonts` 中 value 文本先覆盖后重置，被全局字号抹掉（与 Kps/Total 段顺序相反）。两变体均已修正顺序。
- **速度 ≤ 0 时雨滴永不回收**：键入值不钳制存储，零/负速度永远无法把雨滴抬出轨道顶——关闭淡出时每次按压永久泄漏一枚不可见雨滴（内存与逐帧成本持续增长）。速度取下限（近乎冻结但保证回收），高度/宽度下限 1。
- **每键 KPS 关闭时队列只进不出**：入队不受显示开关门控而清理受门控——默认配置下每键每按压泄漏 8 字节整场累积。入队现受门控。
- **拖动滑块每秒约 120 次写盘**：每次 IMGUI 变更事件都执行双文件全量保存（profile JSON + meta）。GUI 处理器改走去抖保存（静默期后首次变更立即保存；快速连续变更合并，松开鼠标或 0.5 秒后落盘）。关键路径（关窗/禁用/场景加载/Profile 操作）仍立即保存。
- **雨滴高度/速度/宽度滑块从不保存**：18 个直赋值滑块（普通+鬼雨）仅靠其它变更触发保存时捎带落盘——刚调完就崩溃会整组回退。现走去抖保存。
- **鬼雨宽度滑块显示排名标签**：从起始 Y 区块复制而来；现与普通雨宽度块一致使用宽度文案。
- **每键 KPS 开关重开 ≤1 秒显示旧计数**：切换时未清变化检测缓存，队列排空前显示旧值。现已清缓存。
- **负按压缩放渲染镜像垃圾**：可见矩形守卫跑在缩放前，键入负缩放产生负宽高矩形。mesh 构建改在缩放后复查（同时拒绝 NaN）。
- **ppu 缩放方向写反**（`×ppu/100` 应为 `×100/ppu`，形状与雨滴渲染器同错）且九宫格内圈 UV 误用缩放后边框——随包 100 ppu 贴图下惰性，现对非默认导入正确。
- **雨滴池出池重置遗漏 5 个渲染字段**（阴影/描边颜色与偏移）——当前无害（所有创建路径都全量写入），现对称重置以绝后患。
- **DisableKeyViewer 不清活跃雨滴状态**——正确性此前押注"雨滴索引 < 24 < 任意数组长度"的隐式不变量；现显式清理。
- **其它加固**：`MigrateV1toV2` 幂等（版本字段丢失时已归一化坐标不再被二次除以参考分辨率）；`DeleteProfile` 切换失败时中止删除；重绑定按键本身不再被计数（主键与鬼键均吞边沿）；`Main.Init` 防重复订阅；移除三处脚键双重重建；按键绑定主键区/鬼键区补齐缺失的边界守卫。
- **已知局限（文档化）**：按键检测每帧轮询一次 `Input.GetKey`，极低帧率下同一帧内完整的按下+松开可能丢失。已在 README 说明；修复需引入事件型输入源，超出本轮范围。
- **任意配置下每次启动都原生崩溃（0xc0000005，已离线定位根因）**：走 Newtonsoft 保存 Profile 时会遍历 `Color`/`Vector2` 的契约，读取 Unity 计算属性（`linear`、`grayscale`、`gamma`、`maxColorComponent`……）——它们是 ECall icall，从 Newtonsoft 编译的表达式取值器里调用会让 Mono 静默死亡（无异常、无日志，游戏在 `[Manager] Spawning.` 之后、首次场景加载保存的瞬间直接消失）。之前的 Fields 模式解析器**并不能**阻止这一点——在 `CreateProperties` 里强制 `MemberSerialization.Fields` 后，Unity 结构体的契约里仍然带属性（由离线 harness 实证：用游戏同款 Newtonsoft 13.0.2 + 构建出的 mod DLL 复现完全相同的加载→保存路径，捕获 `Error getting value from 'linear' on 'UnityEngine.Color'`，正是游戏内原生崩溃的孪生兄弟）。修复：Unity 结构体类型改走显式 `UnityStructConverter`（只读写 r,g,b,a / x,y / x,y,z,w——完全绕开成员遍历），mod 自己的数据类带 `[JsonObject(MemberSerialization.Fields)]`，解析器整个移除。过渡构建的转义字符串载体（`CustomNodesJson`/`LayerGroupsJson`）仍在数组为空时一次性导入（用户的 14 节点布局正是这样救回的），列表属性保持 `[JsonIgnore]`，定位用的启动面包屑日志已移除。
- **同一崩溃的连带清理**：此前把配置隔离成 `*.corrupt` 的成员重名（"A member with the name 'CustomNodes' already exists"）源于持久化数组字段改名 `"CustomNodes"` 与同名列表属性并存；上述特性从源头解决。
- **FreeMake 编辑器节点级按压缩放**：显示页的按压缩放现在可以按按键配置——`PressAnimEnabled` 让单个节点退出（全局开关仍是总闸），`UseCustomPressAnim` + `PressAnimScale` 为该节点单独覆盖全局缩放值（钳制 0.3–2.0，按键也可以按压放大）。加上此前已是节点级的隐藏计数、隐藏文字、每键 KPS、字号、不透明度、配色、隐藏节点——显示页的每键控制现已全部落在节点上。
- **FreeMake 内置布局预设（非破坏性）**：工具栏新增「预设」按钮，展开 12K/16K/20K/10K/8K/14K/24K 条带——应用即**新建一个 Profile**（名为「16K-预设」之类，全局设置克隆自当前配置），把固定布局的硬编码排布（前排、后排扩展、标准位置的 KPS/Total，设置了脚键布局时连脚键一并生成；键位取自当前配置的绑定数组、雨滴排与固定布局一致）作为可编辑节点填入并切换过去。预设节点还会从源配置的槽位数组继承每键计数与自定义文本（前/后排按按键序号、脚键按 FootKeyBase 起的槽位）——把固定布局转成预设节点不再清空按键统计与文案；此前绑定映射漏了 Key16 分支，16K 预设节点会错拿 key24 的绑定。**当前布局原封不动**；切换时清空编辑器撤销栈，防止 Ctrl+Z 把旧配置的节点写进新配置。「清空画布」按钮保持可撤销的直接清空。108K 预设含完整 105 键（共享槽位表 + 56px 列步进换算，竖长键跨两行，开启了全键盘 KPS/Total 时连带两块面板）；节点上限随之 40→112，每键配色数组在自定义布局下不再按槽位索引（防越界）。
- **节点级雨滴阴影/描边**：雨线页的阴影（开关、颜色、偏移）与描边（开关、颜色、宽度）设置现在可以在 FreeMake 编辑器里逐节点覆盖——默认关闭，节点跟随所选雨滴排的设置；鬼雨有意保持鬼雨排设置（无节点级鬼雨覆盖）。节点颜色未设置时回退排颜色。加上既有的节点级雨滴宽度/高度/速度、双色渐变与偏移，固定布局按排暴露的每个雨滴参数都有了节点级逃逸口。
- **KPS/Total 文本布局节点级化**：面板节点在编辑器中新增「自定义文本布局」覆盖（居中 / 上下堆叠 / 借节点「隐藏文字」切换仅数值），优先于全局的居中/堆叠/隐藏标签开关——默认关闭，既有面板继续跟随显示页。模式解析（`StatTextMode`）由构建路径与 `SetKpsTotalDisplay` 共用，运行时文本与构建的文本对象始终一致。**开启覆盖时以当前生效的全局模式做种子**——首版直接落到字段默认值（平铺），开启瞬间居中/堆叠像"消失"了一样。
- **显示页与自定义布局的冲突（实测修复）**：按压缩放从不作用于自定义按键（动画只在固定布局输入路径启动——`ApplyCustomKeyEdge` 现在在每次按下/松开边沿启动同一协程，图片按键一并生效）；计数器弹跳只动画计数文本、而 HideCount 时它是隐藏的——现在动画当前可见的文本（计数隐藏时为标签）；全局「隐藏主键计数」会无视节点设置把所有自定义键的数值文本整个去掉（该门控改为仅固定布局——节点的 HideCount 是唯一权威）；「隐藏 KPS/Total 标签」现可作用于自定义面板（跟随 `KpsTotalIsSlim`，标签隐藏且未居中时进入仅数值模式）；自定义布局下隐藏「自定义位置」滑块、下移开关与隐藏主键计数开关，并提示去 FreeMake 编辑器调整（主键滑块驱动的 `ResetKeyViewerPosition` 对自定义布局空操作）；自定义布局不再初始化脚键（残留脚键样式会把固定位置的脚键画进节点画布——且 `ResetFootKeyViewer` 这条 `ResetKeyViewer` 内部的第二路径仍往 FootKeyBase(24) 起的脚键槽位写、而自定义布局的 Keys 数组只有节点数那么长，从固定布局切到无节点的自定义时 OnGUI 抛 `IndexOutOfRangeException`）；手改配置中空/短的贝塞尔曲线重置为默认曲线，不再逐帧抛异常。

### 🐛 Bug 修复
- **滑块文本框无法输入中间值**：旧版每事件重灌 `value.ToString`，半输入的值（""、"-"、"0."）被立刻冲掉——雨滴阴影 X/Y（-10~10）根本无法用键盘输入负数；且存储值低于下限时，打开标签页就会被静默钳到下限并写回。现在文本框带输入缓冲（与颜色选择器同一模式），仅当文本真正变化才写回模型；文本输入完全不钳制（负值与超出滑块两端的值原样存储，滑块仅作范围指示），但 "NaN"/"Infinity" 字面量会被拒绝（NaN 会绕过所有 `<=0` 守卫直接毒化 mesh 顶点）。
- **旧版 Profile 迁移崩溃并覆盖用户配置**：profile 重构到 MaxKeySlots=40 之间的版本写入 `Count[36]`，V3→V4 脚键基线迁移的 `Array.Copy` 越界抛异常，异常路径把全部设置重置为默认值并随下次保存覆盖 Profile 文件——永久丢失。现在 `EnsureSettingsArrays` 对 `Count`、`PerKeyGhostRainColor`、`PerKeyFontSize`、`key108` 全部按目标长度重定（此前只查空指针），批量迁移其它 Profile 文件同样先重定长度。
- **加载失败直接吞掉用户配置**：解析失败或迁移异常时回退默认值，但之前先把出问题的 settings.json 与当前 Profile 备份为 `*.corrupt`；配置写入全部改为原子写（临时文件 + 替换），崩溃/断电在写入中途不再截断文件。
- **按住途中关闭按压动画会卡在缩小状态**：释放过渡受开关门控，关闭开关时现在会重置所有按键缩放并停止进行中的动画协程。
- **鬼雨按住时关闭再开启后失步**：旧版雨滴开关关闭时完全不跟踪鬼键状态，重新开启后按住的键要再松/按一次才恢复触发。现在状态跟踪不受开关门控。
- **Profile 折叠展开期间每帧扫盘**：列表同步从"展开期间每个 GUI 事件都执行"（IMGUI 每帧多次）改为展开那一刻执行一次。
- **16K 显示空「第3排」标签**：每键颜色/每键字号的第三排判断 `>=8` 与按键绑定页 `>8` 不一致，16K（恰好 8 个后排键）会画出没有按钮的标签。统一为 `>8`。
- **108 键下误显每键字号区块**：该区块面向标准布局（GetKeyCode/GetBackSequence 与 108 键不对应），全键盘下隐藏。
- **24K 按键列表顺序与屏幕相反**：BackSequence24 末两位 {23,22} 与实际布局（左→右 …22,23）不符，绑定/配色/字号列表按错误顺序显示。修正为 {22,23}。
- **未知语言时高亮错位**：手改配置出现未知语言代码时 I18n 按英文渲染，但语言按钮高亮韩语。现在未知一律按英文。
- **Profile 重名检查大小写敏感**：NTFS 上 "MyProfile"/"myprofile" 是同一文件，重命名时可能先删掉同名变体的文件。改为对**其它** Profile 大小写不敏感检查；仅改大小写的重命名仍允许（旧版这条路会先删源文件再移动导致丢失，现改为直接 File.Move 完成大小写变更）。
- **108 键下无法编辑 KPS/Total 自定义文本**：该编辑器自功能上线（v1.7.0）就放在「按键」标签页尾部，而页首的全键盘提前 return 把入口藏了起来——但标签本身在全键盘 KPS/Total 框里正常渲染，属于功能可达性回归。现在 108 键下按键页保留 KPS/Total 文本编辑区。
- **自定义按键文本无法用退格清空**：删到最后一个字符的瞬间，存储的空值立即映射回默认键名，输入框跳回 "A" 之类，只能全选重打或用重置按钮；且清空那一帧屏幕按键文字会闪空白。两个文本框改为输入缓冲模式（与滑块/颜色选择器同款）。
- **108 键布局下「按键」标签页整页空白**：现在显示"全键盘布局下不支持改键与自定义文本"说明（三语言词条）。
- **淡出时长输入 0 时首帧 NaN 透明度**：`0/0` 产生的 NaN 会绕过 Clamp01，现在零/负时长按立即淡出处理。
- **每场景字体恢复是死逻辑**：OnSceneLoaded 重置了 fontRestored 但 RestoreFontOnce 只在 Start 调用一次。现在场景加载末尾真正调用。
- **Unity 假空检查**：`UpdateAllFonts`/`ExecuteCountReset` 对 MonoBehaviour 使用 `?.` 绕过 Unity 销毁检查，改为显式判空；`ClearActiveDrops` 补齐空守卫。
- **FileBased 变体资源缺失静默**：字体文件（MapleStory/CJK）或 GhostRain.png 缺失时无任何日志——CJK 缺失还会破坏后备链导致中文渲染成方块。现在与 bundle 变体一致记录错误。
- **切换布局/Profile 时脚键重建两遍**：ResetKeyViewer 内部已重建脚键，外层重复调用只做无用功。已去除。
- **普通雨起始偏移滑块从不保存**：三个「起始偏移」滑块只赋值不走去抖保存（页面其它雨滴滑块、包括正下方的鬼雨起始偏移块都已保存）——刚调完就崩溃会回退。现与其它滑块同走去抖保存。
- **V3→V4 批量迁移丢失脚键 14/15 的每键颜色**：36-era 版本写入的 Profile 里 `PerKey*` 颜色数组同样只有 38 长，而批量平移只写旧长度之内——休眠的 16K 脚键 Profile 会丢最后两个槽位（`Count` 在该处已重定，颜色数组没有）。批量迁移现在先重定全部 7 个颜色数组，尾部用该 Profile 自己的全局色填充。
- **切换 Profile 失败可能复活旧长度数组**：回退重载旧 Profile 跳过了 `EnsureSettingsArrays`（成功路径会执行）。仅当刚重写过的文件读不回来（写盘失败/外部改动）才可达，但那会让 `Count[36]` 进入运行态，`RefreshAllCountDisplay` 与每键颜色编辑器越界索引。回退路径现在执行同样的重定/钳制。

### 🔧 性能
- **KPS/Total 更新路径零分配**：`KpsTotalIsSlim` 每次按键都经 `GetLayout` 新分配 LayoutDesc+ExtraSlot[]（隐藏标签模式下每按两次）。结果只取决于布局+标准宽度，已加缓存。
- **设置界面 GUIStyle 缓存**：每键颜色按钮循环里逐调用 `new GUIStyle`（约每帧 26 次分配）及红字按钮样式全部缓存；`PerKeyTypeOrder` 数组静态化。
- **bundle 重建防边框丢失**：`.meta` 未纳入版本管理，新克隆项目重建 AssetBundle 会以 0 边框导入 PNG，静默破坏九宫格按键框与鬼雨平铺。构建脚本现在强制三张贴图的 11px spriteBorder + 100 ppu。

### 🧰 工程与发版
- **csproj 迁移 SDK 风格**：6 个工程由经典格式迁移为 `Microsoft.NET.Sdk`。手写 `Properties\AssemblyInfo.cs` 仍是程序集特性与版本号的唯一来源（`GenerateAssemblyInfo=false`——`/p:Version` 依旧无效，SetVersion.ps1 照常工作）；输出保持 `bin\Release\`、无 TFM 子目录（`AppendTargetFrameworkToOutputPath=false`），CI 拷贝步骤零改动；保留 `LangVersion latest`（SDK 对 net481 默认 C# 7.3）；FileBased 改用通配链接 + 显式排除 `KeyViewerResources.cs`，取代 19 条手写链接清单。新增 `Microsoft.NETFramework.ReferenceAssemblies` 包引用（PrivateAssets），构建机不再需要安装 .NET Framework 4.8.1 Developer Pack。已用 dotnet build 与 CI 同款 MSBuild 双路径全量重建验证：0 错误 0 警告，6 个 DLL 版本均为 1.7.0.0，输出目录干净。
- **版本号一键统一**：新增 `tools/SetVersion.ps1`，一条命令改齐 6 个 AssemblyInfo、`[MelonInfo]` 版本串、两个 Info.json、Repository.json（含下载 URL），幂等可重复运行。发版工作流在构建前自动执行同一脚本——此前 `/p:Version` 对经典（非 SDK）csproj 无效，产出的 DLL 版本号从未变过；同时删除了「打包 zip 之后才改 Info.json」的无效步骤。
- **会话中途改分辨率立即重排**：全键盘 KPS/Total、108 键整体偏移与自定义位置基点此前只在构建/拖滑块时按当时的分辨率固化，改分辨率后一直错位到下次重建；现在检测到分辨率变化立即重新应用。108 键整块的自然槽位与分辨率无关，重排仅重应用 KPS/Total 归一化位置与（开启开关时的）自定义偏移。
- **移除误提交的 Unity 生成工程文件**（`Assembly-CSharp-Editor.csproj`，随编辑器版本自动重新生成），并加入 .gitignore。
- **无法单独关闭某一排雨滴**：旧版把第 1、2 排的雨滴开关都挂在「第2排」开关上，「第1排」开关完全无效。现在三排各自独立生效（0-7 键第1排，8-15 第2排，16+ 第3排），且关闭某排会立即清掉该排飞行中的雨滴。
- **长按鬼键时鬼雨贴图错误**：鬼雨贴图（GhostRain）带 11px 九宫格边框，旧版 `Image.Type.Tiled` 对带边框贴图只平铺内侧区域、边框条沿各自方向重复；合并渲染后误把整张贴图当平铺单元，鬼雨列超过 100px 后每一段都重复出现边框。现按 uGUI `GenerateTiledSpriteToVertexBuffer` 原算法移植：中心内区整块平铺、左右边框列纵向平铺、上下边框行横向平铺、四角各一次，矩形容不下边框时按比例压缩。
- **FileBased 变体字号滑块不实时生效**：散装贴图变体的 `UpdateAllFonts` 漏写 `fontSizeMax`（bundle 变体有），拖动按键字号滑块时已有文本不更新，需重建布局才生效。
- **关闭按键显示后修改设置报错**：显示关闭时在设置窗口改布局/标准宽度/脚键/居中/配色等会触发 NullReferenceException（重建与全局换色在覆盖层不存在时仍然执行）。现在这些操作在显示关闭时安全跳过，下次启用时按新设置构建。问题在重构前就存在。
- **MelonLoader 关窗不保存**：Melon 侧保存事件为空实现，雨滴高度/速度/宽度、起始 Y、KPS/Total 标签等依赖关窗保存的设置只有退出游戏才落盘，中途崩溃即丢失。现在隐藏设置窗口时直接保存；UMM 禁用 Mod 后设置窗口也不再继续在已销毁组件上运行。

### 🔧 性能重构
- **按键框合并渲染**：所有按键的背景/描边不再每键各建两个 Image GameObject，而是合并进两个自绘 mesh（背景层 + 描边层，共享槽位状态，九宫格切片）。主画布从数百个 Graphic 降到 2 个，按下变色/缩放只重建一个小 mesh，不再全画布重批。
- **文本独立子画布**：全部按键文本移入 TextLayer 子画布，KPS/计数的文本更新不再与形状层互相触发重批；按压缩放动画同步作用于文本包裹层与形状 mesh（含「雨滴跟随缩放」模式）。
- **雨滴合并渲染**：删除每键 RainLine 子画布（~25 个嵌套 Canvas）与整池 Rain GameObject，全部雨滴画进两个自绘 mesh（普通雨/鬼雨层）。雨滴状态为纯数据（对象池 RawRain），每帧只更新矩形与渐变参数并重写顶点，无逐雨滴 RectTransform 写入与 SetSiblingIndex。雨滴起始 Y、鬼雨偏移改为渲染时实时读取设置，滑块直接作用于现有雨滴。
- 视觉行为保持：九宫格边框换算（旧版 2 倍尺寸 + 0.5 缩放）、雨滴轨迹渐变/阴影/描边数学、鬼雨带边框平铺布局（见上条修复）；同键内鬼雨（含阴影/描边）仍在普通雨之上。已知差异：① 不同排雨柱重叠时（宽雨/共享列场景），旧版按排交错绘制，现在所有鬼雨统一绘制在最上层；② 雨滴颜色带透明度且开启淡出时，旧版松手瞬间跳到全不透明再淡出，新版从原透明度平滑淡出；③ 鬼雨贴图加载失败时退化为鬼雨色纯色柱（旧版回退为不透明白色）。108 键、脚键、KPS/Total、每键配色、主播模式等全部沿用原逻辑。

---

## v1.7.0

### 🚀 新功能
- **隐藏 KPS/Total 标签**：新增「隐藏 KPS/Total 标签」开关。开启后 KPS/Total 框的 "KPS"/"Total" 文字消失，数值居中显示在框中间，每帧实时更新。仅非扁平（10K/12K/20K 标准模式）布局显示该开关。
- **KPS/Total 堆叠显示**：新增「KPS/Total 上下堆叠」开关，仅在居中模式开启时可用。标签在上方（紧凑字号）、数值在下方，两者在框内上下堆叠互不遮挡。
- **设置界面多标签页重构**：设置窗口拆分为「常规」「布局」「显示」「雨线」「按键」「颜色」六个标签页，顶部常驻主开关；各标签页内容拆分为独立 partial 类文件，界面更整洁、代码更易维护。当前标签页索引持久化到配置，跨会话记忆。
- **颜色选择器支持十六进制输入**：颜色字段现在可以直接粘贴或输入 `#RRGGBB` / `#RRGGBBAA`，自动解析为 Unity 颜色。输入框获得焦点时不再被重置，每个通道有独立的控件名。
- **脚键文本居中显示**：脚键文字不再贴顶，改为整框居中显示，视觉更均衡。

### 🐛 Bug 修复
- **Profile 切换后数据残留**：切换 Profile 时先用 `new ProfileData()` 清空旧数据，再 `FromJsonOverwrite`，避免旧 Profile 的字段泄露到新 Profile。
- **切换 Profile 后尺寸不生效**：`ResetKeyViewer()` 末尾重新应用 `KeyViewerSizeObject` 的缩放，防止 Profile 的 Size 配置被之前的缩放值覆盖。
- **切换布局后居中文本不恢复**：在某个布局开启居中后切到别的布局，现在能正确恢复成正常的标签/数值布局。
- **隐藏主键计数时跳过计数更新逻辑**：`HideMainKeyCount` 为 true 时不再创建 value 文本，也不会尝试更新它。
- **全键盘布局切换后 KPS/Total 状态保持**：切换主键布局后全键盘 KPS/Total 的显示/隐藏状态不再丢失。

### 🔧 性能
- 配置访问更流畅，雨滴对象池优化。

---

## v1.6.5

### 🚀 新功能
- **108 键全键盘布局**：新增一种完整物理键盘布局（含小键盘）。按键间距统一为 6px，数字键盘紧贴主键区，小键盘的 `+`/`Enter` 高键跨两行。该模式下无脚键、无每键配色、无鬼键。
- **全键盘专属 KPS/Total**：全键盘拥有独立的「显示 KPS/Total」开关，外加独立的位置控制和尺寸滑块（40–400px）。「全键盘统一色」选项让 KPS/Total 共用键盘的背景、描边、文字颜色。
- **整块键盘自由移动**：自定义位置滑块现在能整体平移 108 键。拖动 X/Y 即可把键盘贴到任意屏幕边缘（左/右/上/下）；KPS/Total 保持各自独立位置，不会被一起拖动。
- **KPS/Total 文本居中**：新增「KPS/Total 文本居中」选项。对左右扁平的 KPS/Total 框，它会把标签和数值合并为一行居中显示，且数值变长时整行重新居中（如 `KPS 123`）；上下堆叠的布局保持标签在上、数值在下，并隐藏该开关。
- **全键盘设置更干净**：全键盘模式下隐藏无关的开关（隐藏主区域计数、每键 KPS、主播模式），KPS/Total 控制独立成折叠区块，位于颜色设置上方。
- **游戏内重绑设置热键（MelonLoader）**：设置窗口新增「设置界面热键」一行，点击按钮后按任意键即可重绑开关设置界面的热键，无需再手动改 `MelonPreferences.cfg`。

### 🐛 Bug 修复
- **全键盘开启自定义位置崩溃**：在 108 键布局下开启自定义位置不再报错。
- **全键盘误显脚键**：开启自定义位置时，全键盘模式不再出现脚键控件或残留按键。
- **切换布局后居中文本不恢复**：在某个布局开启居中后切到别的布局，现在能正确恢复成正常的标签/数值布局。

### 🔧 性能
- 设置访问更流畅，雨滴对象池重写（容量更大、无残留雨滴）；按键计时的内部队列略有扩容。

## v1.6.4.1

### 🚀 新功能
- **双加载器支持**：同时适配 UnityModManager 和 MelonLoader。核心 DLL 加载器无关，通过 IModLoader 接口桥接。新增 4 个薄层加载器项目
- **MelonLoader 设置界面**：按默认热键 F1 唤出设置窗口（可自定义），窗口大小随分辨率自适应
- **MelonLoader 热键可配置**：编辑 `UserData/MelonPreferences.cfg` 中 `[JipperKeyViewer]` 节的 `Hotkey` 字段
- **`[MelonGame]` 已移除**：可在任何 MelonLoader 游戏下加载（TMP 支持为前提）
- **按键字号滑块**：新增 `KeyFontSize` 滑块（8–72），替换硬编码 `fontSizeMax=20`。Tab(⇥)/Space(␣) 直接拉大字号即可，不再需要 `<size>` 标签

### 🧹 重构
- **Main.cs**：加载器无关化，`Init(IModLoader)` + `EnableNow()` 替代原有 UMM 绑定
- **ModLoader.cs**：新增 `IModLoader` 接口 + `Loader` 静态访问类
- **KeyViewerResources.cs/I18n.cs 等**：所有 `Main.Mod.*` 引用替换为 `Loader.*`
- **Info.json**：入口指向 `JipperKeyViewer.Loader.UMM.dll`
- **Build 流程**：6 项目构建 + 打包含两种加载器 + 两种变体

### 🐛 Bug 修复
- **按键按下动画**：按下时按键平滑缩小，松手恢复（80ms lerp 协程）。`EnablePressAnimation` 开关、`PressAnimationScale` 缩放比例（0.5–0.95）、`EnablePressAnimationOnRain` 控制雨滴是否跟随。Visuals 包裹层隔离缩放，雨滴容器不受影响
- **每键鬼键雨滴颜色**：新增 `PerKeyGhostRainColor` 颜色数组，每键颜色编辑器中增加鬼键雨滴色块，独立于普通雨滴颜色

### 🐛 Bug 修复
- **14K 后排按键位置修正**：按"16K 裁掉最外两列"重排，`BackSequence14={13,9,8,10,11,12}`，键位居中于 columns 1–6（x=54–324）。切换布局时各键计数位置不变
- **雨滴容器不共享**：每个按键保留自己的 RainLine，`ShareRainContainer` 只调 X 偏移和宽度对齐前列，不再销毁/重定向容器。消除排间 Z-order 问题，不再需要 Canvas.sortingOrder hack
- **移除 Canvas.sortingOrder**：`FixRainContainerSortOrder()` 已删除——导致雨滴覆盖父级按键层级。同时移除 RainLine 上不必要的 `GraphicRaycaster`
- **阴影跟随顶部渐变**：`RainGraphic` 阴影从 `AddQuad` 纯色块改为 `DrawRainQuad`，和描边、主体一样受渐变影响
- **24K 后排序列修正**：`BackSequence24` 修正为正确 layout 顺序
- **AutoAssignRainbowColors 重构**：按当前布局键数 + 脚键 + KPS/Total 分配颜色，不再用顺序索引
- **动画协程 null 保护**：添加 `if (animTarget == null) yield break;`，防止销毁后的按键空引用崩溃
- **加载时 NRE 修复**：颜色数组改用 `SafeEnsure` 初始化，`InitPerKeyColors` 雨滴颜色循环使用 `footBase`

### 🧹 重构
- **FootKeyBase 固定为 24**：移除条件判断（原 `Key24 ? 24 : 20`）。V3→V4 迁移将脚键数据从 index 20 移动到 24（含多 Profile 迁移）
- **Image 版雨滴阴影/描边**：Rain 对象添加 `shadowImage`/`outlineImage` 子对象 + `SetupShadow`/`SetupOutline` 方法，渐变纹理同步到三个 Image
- **EnsureColorArray/SafeEnsure**：统一辅助函数替代逐个字段的空值检查
- **PerKeyTypeOrder 扩展**：主键增加 type 7（鬼键雨滴颜色）
- **Settings.Version 升级**：3 → 4，支持脚键基地迁移

## v1.6.3

### 🚀 新功能
- **24K 布局**：新增 24 键布局（8+8+8），三排完整的 50px 等距排列。脚键索引扩展至 24-39，24K 下最多可选 16K 脚键。完整支持鬼键绑定、第三排雨滴/鬼雨设置。布局参数 frontY=375、KPS/Total 在 y=221，行间间隙 6px（与 16K 一致）
- **标准按键宽度模式**：开关 `StandardKeyWidth` 将宽窄混排的后排统为 50px（12K 宽键 77→50、10K 129→50、20K 第三排 77→50），KPS/Total 拉宽填平两端
- **鬼雨独立高度/速度/宽度**：新增 `GhostRainHeightRow1/2/3`、`GhostRainSpeedRow1/2/3`、`GhostRainWidthRow1/2/3` 滑条，鬼雨参数完全独立于普通雨

### 🐛 Bug 修复
- **12K/10K 雨滴位置**：非标准宽度后排键（12K key8/10=77px, 10K key8/9=129px）共享前排 RainLine 容器（12K: 9→2,8→3,10→4,11→5；10K: 8→3,9→4），雨滴与前列对齐
- **雨滴渲染顺序**：共享容器内后排雨滴被前排遮挡。`SetSiblingIndex((row-1)*2 + isGhost)` 固定渲染层级：前排=0、中排=2、第三排=4
- **14K 布局跳变**：16K 切 14K 时整体下沉 21px。14K 全部 Y 值归一到 16K（frontY 299→320, 后排 245→266, KPS 199→220）
- **8K 开启放下面后偏高**：8K 重新定位为"16K 去掉第二排"——frontY=266（16K 第二排行）、KPS/Total y=220（16K KPS 位置），放下面后 KPS 也在 y=20 与 16K 对齐
- **20K 第三排鬼键 UI 顺序反了**：按钮显示 16,17,18,19，但实际键位 17,16,18,19。改用 `BackSequence20[8..12]`
- **自定义位置折叠误当开关**：折叠按钮同时控制开关，无法展开查看滑条而保持功能开启。拆为独立折叠状态 + 内部勾选框
- **DrawMainKeyRows 第三排越界判断**：循环条件 `i < keyCodes.Length` 检查的是循环下标而非 `backSequence[i]`
- **24K 第三排雨滴不触发**：`IsRainEnabledForKey` 上限为 index 20，阻断 20-23。改为 `keyIndex < FootKeyBase`
- **24K KPS/Total 与第三排重叠**：KPS 在 y=165 与第三排（y=232）重叠。上调至 y=221，间距 6px
- **24K 切出后残留旧对象**：切换回非 24K 布局时 Keys[20-23] 残留旧 24K 主键对象。`ChangeKeyViewer()` 同时调用 `ResetFootKeyViewer()` 重建脚键
- **24K 脚键限制解除**：`MaxKeySlots=40` 允许 24K 选择 14K/16K 脚键（索引 24-39）
- **设置加载崩溃**：`InitPerKeyColors` 在 `Settings` 未赋值时调用 `FootKeyBase` 导致 NullReferenceException。改用 `MaxKeySlots` 常量

### 🧹 重构
- **雨滴容器共享**：`ApplyRainContainerSharing()` / `ShareRainContainer()` 将后排 `key.rain` 重指向前排容器。`UpdateRainContainerPositions` 用 `HashSet<RectTransform>` 去重
- **动态脚键基线**：`FootKeyBase` 属性替代全库硬编码 20（24K 为 24，其余 20）。17 处脚键索引同步更新
- **MaxKeySlots=40**：所有键位数组（`Keys`、`Count`、`keyPressTimes`、`lastPerKeyKps`）使用 `MaxKeySlots`。每键颜色数组使用 `MaxKeySlots + 2`。`KeyIndex` 返回 `Keys.Length`/`+1` 代表 KPS/Total。移除全库硬编码 36/37/38/20
- **HasThirdRow 属性**：替代 18 处 `style == Key20` 判断，覆盖 20K 和 24K

## v1.6.2

### 🚀 Features
- **每排雨滴起始高度**：`RainStartYRow1/2/3` 滑块替代硬编码容器 Y 值（默认 −223/−169/−115），拖动滑块实时预览
- **每排鬼键起始高度**：`GhostRainStartYRow1/2/3` 独立绝对位置滑块，与普通雨滴互不干扰
- **每排雨滴阴影**：每排独立阴影开关、颜色、XY 偏移，鬼键另有独立设置
- **每排雨滴描边**：每排独立描边开关、颜色、描边宽度，鬼键另有独立设置
- **每排雨滴宽度**：新增 `RainWidthRow1/2/3` 独立滑块（10–200px），每排雨滴可单独设置宽度
- **雨滴速度/高度上限提升**：Slider 从 1000 扩展到 2000，text field 上限完全放开
- **韩语支持**：新增 `ko` 语言选项

### 🐛 Bug Fixes
- **Profile 加载字段泄漏**：`LoadProfile` 用 `FromJsonOverwrite` 覆盖已有 `Settings.Data` — 旧 profile JSON 中缺失的新字段会保留上一个 profile 的值。修复：加载前先 `new ProfileData()` 重置所有字段为默认值
- **鬼键不显示**：鬼键 `RainGraphic` 始终启用，阴影描边都关时空网格干扰子节点 `ghostImage`。修复：只在阴影或描边开启时才启用 `graphic`
- **鬼键起始高度轨迹尺寸错误**：`startY` 混入轨迹长度计算，导致鬼键出现时自带一段初始轨迹。修复：`y`（轨迹长度）与 `dropY = startY + y`（位置）分离
- **首次按键雨滴不显示**：`RainSystem.PreWarmPool` 在无有效 Canvas 时预创建对象，移除预创建
- **Color foldout 每帧重置子项状态**
- **KPS/Total sub-foldout 判断**：用 `>=0` 代替 `==t`

### 🔧 Performance
- **零分配 KPS/Count 显示**：`NumBuffer.Format` 预分配 char[32] + `TMP_Text.SetText`，消除每帧 `ToString`
- **Queue 预分配**：`PressTimes` (256) 和 per-key KPS 队列 (32) 预分配
- **Idle skip**：`_hasKeyPressActivity` 标志位，无按键时跳过循环

### 🧹 Refactor
- **Profile 系统**：`KeyViewerSettings` 拆分为 `ProfileData` + 元数据壳，支持多 Profile 创建/切换/重命名/删除
- **GUI 重构**：提取 `FloatSliderField` / `DrawFoldoutButton`；`DrawSettingsWindow` 拆分为 10+ 独立方法
- **RainSystem 重构**：`UpdateEffects` 拆分为多个独立方法
- **AddQuad 参数简化**：7 参数 → 4 参数
- **I18n 重写**：手写 JSON 解析器替换为 `JsonUtility`
- **FileBased 字体样式**：补齐 `FontStyleFlags` 支持

